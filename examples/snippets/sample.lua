-- Snippet playground. Try these in insert mode (see init.lua for the full list):
--
--   * type `fn`  then <C-n> <C-y>  -> expands a function, cursor on the name;
--                                     <Tab> jumps name -> args -> body.
--   * type `for` then <C-n> <C-y>  -> a numeric for loop with three tabstops.
--   * type `loc` then <C-n> <C-y>  -> `local x = x`; edit the name and watch the
--                                     mirror on the right update in lockstep.
--   * press <C-s> anywhere in insert mode -> expands print(${1:value})$0 directly.
--
-- Scratch space below:

local M = {}

function M.demo()
end

return M
