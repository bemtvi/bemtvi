# npm install artifacts and the generated highlighter assets. package.json + the
# committed package-lock.json are tracked; these are regenerated by build.sh, never
# committed. ./vendor/ is copied into ../web/vendor/ by the parent build.sh.
/node_modules
/vendor
