nxvim synchronous prompts — vim.fn.input + vim.fn.confirm playground

  <Space>i   vim.fn.input:   type a name at the prompt (<CR> confirms, <Esc> -> "")
  <Space>d   vim.fn.confirm: single-key Yes/No/Cancel dialog (press y/n/c)
  <Space>r   chained:        input() a new name, then confirm() before renaming

placeholder
delete-me

Run  :messages  to review what each prompt returned.
