/node_modules/
/target/
/web/pkg/
/web/tailwind.css
# Generated by `npm run build:treesitter` (build.sh): the web-tree-sitter runtime,
# the per-language grammar .wasm files, and the sanitized highlight queries. All
# regenerated from the pinned grammar devDependencies, so not checked in (~13 MB).
/web/vendor/
